image: cern/cc7-base

pages:
  script:
  - curl --silent --location https://rpm.nodesource.com/setup_12.x | bash -
  - yum install -y nodejs
  - npm install
  - cd site; npm install; ../node_modules/.bin/ng build --prod
  artifacts:
    paths:
    - site/dist/site
